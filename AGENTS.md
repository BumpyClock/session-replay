# Premise
Web app to help users scan their sessions across different agents (Claude Code, Codex CLI, Copilot CLI, Opencode etc.) and replay then and share them in a single HTML file so that other users can watch a replay of their session. 

Current development status = active development.
